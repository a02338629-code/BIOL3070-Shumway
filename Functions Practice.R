?factor()
?plot()
?c()
#### the ? pulls up the help menu, can be removed later
# the help menu has a usage section, which shows 
##required arguments (what goes in the parantheses x,digits=0) 
###and default (what it will use if no argument given)

?round() #pulls up help menu
round(x = 3.141593) #define x, leave digits default)
round(x = 3.141593, digits = 6) #define x and digits
round(x=333.111, digits = 3)

#DAY 2
round(333.111)
?mean()
mean(c(1.5,2.5,3.5,4.5), trim=0.5, na.rm=FALSE)
